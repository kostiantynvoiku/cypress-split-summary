describe('template spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })

  it('waits and passes', () => {
    cy.wait(1000)
  })

  it('waits and passes', () => {
    cy.wait(1000)
  })

  it('waits and passes', () => {
    cy.wait(1000)
  })

})